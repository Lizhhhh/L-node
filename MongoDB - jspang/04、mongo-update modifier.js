const db = connect('company');

// db.worker.update({ "name": "lzhhhh1" }, { "$set": { "skill": ["photoShop", "UI", "WPS"] } });

// db.worker.update({ "name": "lzhhhh1" }, { $unset: { age: '' } });

print('[success] ok');